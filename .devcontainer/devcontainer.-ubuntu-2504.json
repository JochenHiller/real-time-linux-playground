{
  "name": "Ubuntu 25.04 Codespace",
  "image": "ubuntu:25.04", // Or "ubuntu:plucky"
  "customizations": {
    "vscode": {
      "extensions": [
        // Add your preferred VS Code extensions here
        // e.g., "ms-python.python", "dbaeumer.vscode-eslint"
      ]
    }
  },
  // Add other configurations like port forwarding, post-create commands, etc.
  // "forwardPorts": [3000],
  // "postCreateCommand": "uname -a && sudo apt-get update && sudo apt-get install -y curl"
}